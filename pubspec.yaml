name: memory_and_json_directories
description: A platform independent directory structure, which can be saved as JSON
version: 1.5.0
repository: https://github.com/Honeyman-Applications/memory_and_json_directories

environment:
  sdk: ">=2.17.3 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
